/* PresupuestoClaro — recursos/funciones.js
   En esta entrega (Fases 1 y 2) el sitio es solo estructura y estilos.
   Se deja únicamente el esqueleto de carga para la Fase 3, cuando se
   agregue la lógica de interactividad (agenda dinámica, validación
   de formulario, menú móvil). */

window.addEventListener('load', iniciar);

function iniciar() {
  // La lógica de interacción (agregar/quitar ítems de la agenda,
  // validación en tiempo real del formulario) se implementa en la Fase 3.
}
